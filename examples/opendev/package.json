{
  "name": "opendev",
  "version": "0.1.0",
  "type": "module",
  "bin": { "opendev": "./src/index.ts" },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --compile --outfile opendev"
  }
}
